package org.ssglobal.training.codes.itemC;

import java.util.HashSet;
import java.util.Set;

public class Enrollment {
	Set<Student> biology = new HashSet<>();
	Set<Student> comsci = new HashSet<>();
	Set<Student> engineering = new HashSet<>();
	Student student;
	
	public void getMajor() {
		
		if (student.getSubject().equals(biology)) {
			
		}
	}
}
